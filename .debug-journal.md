# Debug Journal - Task 12 runtime prerequisites
Started: 2026-08-14
Goal: Refresh the local runtime to current source and prove authenticated custom-video prerequisites without exposing secrets.

## Environment snapshot (Phase 0)
- Runtime: Go 1.26.5 backend; Node 18.20.8 / Next.js frontend; Docker Desktop services.
- Current frontend: `http://127.0.0.1:3001`, host Next.js development process.
- Current backend: `ic-backend`, host port 18080, image `infinite-canvas-backend:latest`.
- Git HEAD: `e7882744d94e961dd768ce3f3e57decafa52a9a6`; unrelated untracked frontend test left untouched.
- References read: Go runtime, Node runtime, Playwright CLI, setup, investigation, and manual-QA debugging references.

## Hypotheses
1. [OPEN] The backend image is stale - distinguishing evidence: image/container creation is older than the source route, and rebuilding/recreating changes the image ID then changes preset GET from 404 to an authenticated route response - if true, fix is: rebuild backend.
2. [OPEN] The request path is wrong - distinguishing evidence: source route registration and direct/proxied requests disagree on whether `/backend-api` is part of the backend route - if true, fix is: use canonical path.
3. [OPEN] The browser frontend proxies to a different backend than the inspected container - distinguishing evidence: frontend process configuration, container logs, and correlated request status identify different targets - if true, fix is: align runtime target.

## Artifacts to revert
- [ ] This `.debug-journal.md` file. Remove after permanent evidence is complete.
- [ ] Any refreshed service process/container started in this session. Leave the requested current runtime running; record exact lifecycle command in permanent evidence.
- [ ] Any minimal QA model/config data created in this session. Keep it only when required as the requested Task 12 prerequisite and identify it without secrets.
- [ ] Any temporary Playwright/CDP script. Store only sanitized reusable evidence or remove it before completion.
- [ ] `.omo/evidence/task-12-e2e-success/docker-config/config.json` - isolated anonymous Docker client config used to bypass the unavailable desktop credential helper; retain as sanitized evidence.
- [ ] `backend/.task12-server` - temporary Linux backend binary compiled from current source. Remove after the current runtime image is created.
- [ ] Frontend development process started with `bun run dev` on port 3001. Leave running as the requested current local runtime.

## Findings
- Source tree HEAD is newer than the running backend image.
- Running backend image ID: `sha256:2cc55e94e8b6fc7092766e213d43fac6581a8ae075ac2a77b41b57958a39a236`, created 2026-07-20.
- Running backend container was created 2026-07-20 and started 2026-08-12 from the root Compose project.
- Direct backend `GET /api-config/video-presets` and canonical `GET /backend-api/api-config/video-presets` both returned 404 before refresh.
- Authenticated browser session is currently reachable at `/video`; legacy controls are visible and custom role controls are absent before refresh.
- Initial Compose rebuild failed before container recreation because Docker Desktop's credential helper returned `A specified logon session does not exist`; retry uses an isolated empty-auth Docker config.
- The isolated Docker client config did not bypass Docker Desktop's daemon-side credential failure. The fallback keeps the existing local Alpine runtime image and replaces only `/server` with a locally cross-compiled current-source binary.
- Current backend route verification succeeded (`GET /backend-api/api-config/video-presets` changed from 404 to 401 without auth), but the prior Next.js dev process was no longer listening on port 3001 and required a normal `bun run dev` restart.
- `bun run dev` resolves Node 18.20.8 and exits because Next.js 16.2.3 requires Node >=20.9. The equivalent project-defined `next dev --webpack -H 0.0.0.0 -p 3001` command is launched with installed Node 24.14.0.
